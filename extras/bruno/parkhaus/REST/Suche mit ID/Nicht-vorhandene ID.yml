info:
  name: Nicht-vorhandene ID
  type: http
  seq: 1

http:
  method: GET
  url: "{{restParkhaeuserUrl}}/:id"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: id
      value: "0"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
